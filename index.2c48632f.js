// Generated by CoffeeScript 2.7.0
var Overlay, Sidebar, anyMod, i, len, lesFeuillets, openLink, sectId, section, sections, subBar, subItems, title, w3_close, w3_open;

Sidebar = document.querySelector("#mySidebar");

Overlay = document.querySelector("#myOverlay");

w3_open = function () {
  Sidebar.style.display = "block";
  Overlay.style.display = "block";
};

w3_close = function () {
  Sidebar.style.display = "none";
  Overlay.style.display = "none";
};

openLink = function (evt, animName) {
  var article, articles, i, j, len, len1, tablink, tablinks;
  window.scrollTo({
    top: 0,
    left: 0,
    behavior: "smooth",
  });
  articles = document.querySelectorAll(".article");
  for (i = 0, len = articles.length; i < len; i++) {
    article = articles[i];
    article.style.display = "none";
  }
  tablinks = document.querySelectorAll(".tablink");
  for (j = 0, len1 = tablinks.length; j < len1; j++) {
    tablink = tablinks[j];
    tablink.className = tablink.className.replace(" w3-blue", "");
  }
  document.getElementById(animName).style.display = "block";
  evt.currentTarget.className += " w3-blue";
};

subBar = document.querySelector("#subBar");

if (subBar) {
  sections = document.querySelectorAll(".sect");
  sectId = 0;
  subItems = "";
  for (i = 0, len = sections.length; i < len; i++) {
    section = sections[i];
    section.id = ++sectId;
    title = section.querySelector("h2").textContent;
    subItems += `<button class='w3-bar-item w3-button tablink' onclick='openLink(event, ${sectId})'>${title}</button>`;
  }
  subBar.innerHTML = subItems;
}

lesFeuillets = document.querySelector(".feuillets-list");

if (lesFeuillets) {
  anyMod = lesFeuillets.querySelector("p.modified");
  if (anyMod) {
    document.querySelector("#anyMod").classList.toggle("modified");
  }
}
